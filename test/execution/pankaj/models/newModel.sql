WITH LISTING_CONSUMPTION_DAILY AS (

  SELECT * 
  
  FROM {{ source('SNOWFLAKE.DATA_SHARING_USAGE', 'LISTING_CONSUMPTION_DAILY') }}

),

Reformat_1 AS (

  SELECT * 
  
  FROM LISTING_CONSUMPTION_DAILY AS in0

)

SELECT * 

FROM Reformat_1
